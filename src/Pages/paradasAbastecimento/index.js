import './index.scss'
import React, { useState } from 'react';

export default function ParadasAbastecimento() {
    const[capac, setCapac] = useState(0);
    const[consumo,setConsumo] = useState(0);
    const[dist, setDist] = useState(0);

    function paradasAbastecimento() {
    }

    return(
        <main>

            <section>

                <h1> Bem vindo a Paradas Abastecimento! </h1>

                <h2> Capacidade </h2>

            </section>

        </main>
    )
}